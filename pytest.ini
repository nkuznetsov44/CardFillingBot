[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts = 
    -v
    --tb=short
    --strict-markers
markers =
    unit: Unit tests
    integration: Integration tests
    regex: Regex pattern tests
    parsing: Date and currency parsing tests
    formatting: Table and text formatting tests 